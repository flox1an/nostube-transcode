# Required: Nostr pubkey of the DVM operator (npub or hex)
# The DVM will only accept admin commands from this key.
OPERATOR_NPUB=npub1...

# Optional: Bootstrap relays for initial connection
# BOOTSTRAP_RELAYS=wss://relay.damus.io,wss://nos.lol

# Optional: HTTP port for the admin web UI (default: 5207)
# HTTP_PORT=5207

# Optional: Temp directory for video processing (default: ./temp)
# TEMP_DIR=/app/temp

# Optional: Logging level (default: info)
# RUST_LOG=info
